#pragma once

// Cross platform (no pins.h)

enum class UIMode {
    NORMAL,
    PALETTE,
    VOLUME,
    MEDITATION,
    COLOR_WHEEL
};

enum {
    BLADE_OFF,
    BLADE_IGNITE,
    BLADE_ON,
    BLADE_RETRACT
};


