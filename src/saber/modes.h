#pragma once

// Cross platform (no pins.h)

enum class UIMode {
    NORMAL,
    PALETTE,
    VOLUME,
    MEDITATION
};

enum {
    BLADE_OFF,
    BLADE_IGNITE,
    BLADE_ON,
    BLADE_FLASH,
    BLADE_RETRACT
};


