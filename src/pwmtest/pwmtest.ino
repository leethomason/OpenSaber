void setup() {
  // put your setup code here, to run once:
  pinMode(3, OUTPUT);
  pinMode(4, OUTPUT);
  pinMode(9, OUTPUT);
  //analogWrite(3, 20);
  //analogWrite(4, 20);
  analogWrite(9, 255);
}

void loop() {
  // put your main code here, to run repeatedly:

}
